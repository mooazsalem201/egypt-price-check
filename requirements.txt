# curl_cffi impersonates Chrome's TLS/JA3 fingerprint. Carrefour Egypt sits behind Akamai
# Bot Manager, which fingerprints the TLS handshake itself -- plain requests/Scrapy/curl
# all receive a 53-byte empty stub no matter what headers they send.
curl_cffi>=0.7
pillow>=10
